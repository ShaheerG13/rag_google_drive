# Step 1 dependencies
pinecone
python-dotenv
typing_extensions

# Step 2 dependencies (Google Drive via OAuth)
google-api-python-client
google-auth
google-auth-oauthlib

# Step 3 dependencies (text extraction + embeddings)
pypdf
python-docx
openai

# Step 5 dependencies (web UI)
fastapi
uvicorn

# Step 6 dependencies (near-instant sync)
apscheduler
